# ChangelogRecipe reference

See `.claude/skills/fragile-recipe/references/changelog-recipe.md`.

This file used to be a second copy and had already drifted: it was missing the
whole `indexLinkPattern` (two-stage "index → latest") section, so an agent reading
this one would conclude the registry cannot follow a per-version-page index — which
it can. One copy, one place.
